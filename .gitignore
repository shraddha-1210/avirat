.venv/
.env
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.coverage

# Frontend — the app lives in frontend/, but its build output goes to
# static/dist/ at the repo root because that is what FastAPI serves.
node_modules/
frontend/node_modules/
static/dist/
*.tsbuildinfo
